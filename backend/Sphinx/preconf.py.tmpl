{% with buildout=parts.buildout; settings=parts.settings %}

# ${buildout['edit-warning']}

"""Documentation settings to be filled in at the last moment"""

public_url = '[=public_url=]'
api_url = '[=api_url=]'
stream_url = '[=stream_url=]'

{% end %}
