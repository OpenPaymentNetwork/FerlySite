${recipient_name},

${describe_notes_text_caps}, sent to you by ${sender_name}, has been added to your wallet.

% if show_message_once:
The sender said:

${message}

% endif
% if has_loyalty_notes:
You also sent loyalty cash back: ${describe_loyalty_notes_text}.

% endif
${recipient_transfer_url}